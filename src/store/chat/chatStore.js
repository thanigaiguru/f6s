//$Id$

import actions from './chatAction';

let state = {
    
};

let mutations = {
    
};

let getters = {
    
};

export default {
    namespaced: true,
    state,
    mutations,
    getters,
    actions
}